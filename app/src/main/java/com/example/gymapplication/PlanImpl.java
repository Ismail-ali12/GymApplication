package com.example.gymapplication;

public class PlanImpl extends Plan {
    public PlanImpl(Workout workouts, int minutes, String day, boolean isComplete) {
        super(workouts, minutes, day, isComplete);
    }
}
